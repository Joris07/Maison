import pymongo
from pymongo import MongoClient

myclient = MongoClient(host="0.0.0.0",port=27017)
mydb = myclient["db"]
mycol = mydb["temperature"]
mydict = { "Temperature":"19" }

mycol.insert_one(mydict)
